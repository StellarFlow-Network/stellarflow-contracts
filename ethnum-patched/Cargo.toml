[package]
name = "ethnum"
version = "1.5.0"
edition = "2021"
description = "256-bit integer implementation (patched: transmute fix)"
license = "MIT OR Apache-2.0"
repository = "https://github.com/nlordell/ethnum-rs"

[features]
default = []
llvm-intrinsics = []

[dev-dependencies]
serde_json = "1"

[dependencies]
serde = { version = "1", default-features = false, optional = true }
